# SAP WebGUI 连接配置
sap:
  url: "http://your-sap-server:8000/sap/bc/gui/sap/its/webgui"
  client: "100"
  language: "ZH"
  # 认证信息建议通过环境变量传入
  # username: 通过 SAP_USER 环境变量
  # password: 通过 SAP_PASS 环境变量

browser:
  headless: false
  slowMo: 100  # 毫秒，便于观察和演示
  timeout: 30000
