# 发布检查表与评分

## 当前评分

| 维度 | 分数 | 说明 |
|---|---:|---|
| 综合发布质量 | 96/100 | 已从“SAP 项目复盘”升级为“通用框架 + SAP 样例 + Capture Layer + Action Registry + 架构踩坑”的系列 |
| 工程可信度 | 94/100 | 保留 SAP 真实数据、踩坑案例、测试与实现状态 |
| 掘金传播潜力 | 94/100 | 标题从 SAP 专用转向企业后台通用问题，并补了 Page Object 踩坑篇 |
| GEO 友好度 | 95/100 | 每篇都有明确问题、结论、FAQ、可带走清单和实体词，且补齐 Action Registry 关键实体 |
| 风险控制 | 95/100 | 明确说明 SAP 是首个实践样例，不宣称覆盖所有系统 |
| 可复制性 | 96/100 | 增加 Recording Pack、Action Registry、Adapter-first / Page Object-inside 的落地检查清单 |
| 自动化资产生产链路 | 90/100 | 已落地 Recording Pack 规范和模板，Recorder CLI / Compiler CLI 仍是下一阶段 |

## 已完成的关键优化

- 系列主定位从“AI + Playwright 半自主 SAP 自动化”升级为“AI + Playwright 通用企业 Web 自动化框架”。
- SAP 被重新定位为：
  - 第一个真实实践项目；
  - 第一个领域 Adapter；
  - 第一块高复杂度压力测试样例。
- 保留真实工程证据：
  - 3,883 行 TypeScript
  - 10 个 YAML Flow
  - 8 个 Page Object
  - 18 个单元测试全绿
  - 约 4 个 Phase，2 周
- 把系列结构调整为：
  - 01 总论：为什么从 SAP 开始做通用框架
  - 02 SAP 压力测试：为什么需要 Adapter
  - 03 Flow Engine：流程资产化
  - 04 Action Registry：Flow 和 Adapter 的连接层
  - 05 Page Object + Adapter：系统差异隔离
  - 06 AI 角色：意图解析、异常诊断、开发辅助
  - 07 Page Object 踩坑：为什么 Page Object 不是最高抽象
  - 08 Recorder / Capture Layer：自动化资产从哪里来
- 修复 `skills/sap-ui-auto/SKILL.md` 到 v1.3：
  - 引入 Core + Adapter 架构
  - 明确 Page Object 只是 Adapter 内部实现
  - 禁止 Flow 暴露 selector
  - 增加 Page Object 防退化规则
  - 增加 Recording Pack 优先规则
- 新增 Recording Pack V1 落地文件：
  - `docs/recording-pack.md`
  - `recordings/_template/`
- 明确区分已实现和规划中，避免 AI 兜底、Skill、飞书触发被误读为全部落地。
- 继续保留“不要说任何网页 / 任意系统 / 生产级全覆盖”的风险控制口径。

## 推荐发布顺序

1. `01｜别再为每个后台系统重写脚本：我从 SAP 开始做通用 Web 自动化框架.md`
2. `02｜为什么 SAP 是最好的压力测试：iframe、readonly 和动态控件逼出的 Adapter 层.md`
3. `03｜Flow Engine：把企业后台操作从脚本变成可编排流程.md`
4. `04｜Action Registry：让一个 Flow Engine 调用 SAP、OA、CRM 的关键设计.md`
5. `05｜Page Object + Adapter：让通用框架适配 SAP、OA、CRM 和更多系统.md`
6. `06｜AI 不直接接管浏览器：它在通用自动化框架里应该做什么.md`
7. `07｜Page Object 不是银弹：通用 Web 自动化框架的 Adapter 踩坑经验.md`
8. `08｜别再手写第一版脚本：用录制把 SOP 编译成 Flow + Adapter.md`

## 标题备选

### 01

主标题：

```text
别再为每个后台系统重写脚本：我从 SAP 开始做通用 Web 自动化框架
```

备选：

```text
我想做一个通用 Web 自动化框架，为什么第一个样例选了 SAP？
```

### 02

主标题：

```text
为什么 SAP 是最好的压力测试：iframe、readonly 和动态控件逼出的 Adapter 层
```

备选：

```text
我用 SAP 压测通用自动化框架，先踩完了这 8 个坑
```

### 03

主标题：

```text
Flow Engine：把企业后台操作从脚本变成可编排流程
```

备选：

```text
别再把后台自动化写成脚本：用 Flow Engine 沉淀流程资产
```

### 04

主标题：

```text
Action Registry：让一个 Flow Engine 调用 SAP、OA、CRM 的关键设计
```

备选：

```text
Flow 和 Adapter 中间缺的一层：Action Registry 设计
```

### 05

主标题：

```text
Page Object + Adapter：让通用框架适配 SAP、OA、CRM 和更多系统
```

备选：

```text
通用自动化框架最难的一层：如何设计系统 Adapter
```

### 06

主标题：

```text
AI 不直接接管浏览器：它在通用自动化框架里应该做什么
```

备选：

```text
别让 AI 自由点后台系统：它更适合做异常诊断和意图解析
```

### 07

主标题：

```text
Page Object 不是银弹：通用 Web 自动化框架的 Adapter 踩坑经验
```

备选：

```text
我差点把 Page Object 当成框架核心，后来发现 Adapter 才是边界
```

### 08

主标题：

```text
别再手写第一版脚本：用录制把 SOP 编译成 Flow + Adapter
```

备选：

```text
不是录制回放：我为什么要给通用 Web 自动化框架加 Capture Layer
```

## 统一口径

建议持续使用：

```text
通用企业 Web 自动化框架
通用内核 + 领域 Adapter
SAP 是首个高复杂度实践样例
Flow Engine 负责流程编排
Adapter 负责系统差异
Playwright Runtime 负责确定性执行
AI 做意图解析、异常诊断和开发辅助
Action Registry 是 Flow 和 Adapter 的连接层
录制采集，不是录制回放
Recording Pack 是自动化资产生产的输入标准
Adapter-first，Page Object-inside
Page Object 是实现手段，Adapter 是架构边界
内部 PoC / 小规模验证
```

避免使用：

```text
任何网页
任意系统
生产级全覆盖
0-20 分钟接入
维护成本极低
SAP 专用自动化框架
AI 全自动操作 SAP
```

## 发布前还建议补的材料

| 优先级 | 材料 | 作用 |
|---|---|---|
| P0 | 通用框架架构图 | 让读者第一眼理解 Core + Adapter |
| P0 | Capture Layer 架构图 | 让读者理解“采集 -> 编译 -> 执行” |
| P0 | 脱敏执行截图或 GIF | 掘金首图和正文可信度会明显提升 |
| P0 | 一张 HTML report 截图 | 证明不是只在命令行跑通 |
| P1 | 一段 trace 失败复盘截图 | 强化“可复盘、可调试”的工程卖点 |
| P1 | SAP Adapter 目录结构截图 | 让 Adapter 抽象更具体 |
| P2 | GitHub 或脱敏代码片段链接 | 提升收藏和二次传播 |

## 最终建议

这版已经更接近你的真实目标：不是发一个 SAP 自动化项目，而是用 SAP 这个高复杂度样例，推出一套通用企业 Web 自动化框架。

发布前最值得补的是一张概念图：

```text
用户入口 -> 通用框架内核 -> 领域 Adapter -> 具体系统
```

有这张图之后，读者会更容易理解：SAP 是样例，通用框架才是主角。
